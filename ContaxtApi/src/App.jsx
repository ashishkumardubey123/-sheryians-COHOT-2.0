import React from 'react'

function App() {
  return (
    <div>
      hello this is contaxt api 
      <p>Context API is a React feature that allows you to pass data through the component tree without having to pass props down manually at every level.</p>
    </div>
  )
}

export default App
